scrape_configs:
  - job_name: trading_terminal_backend
    scheme: https
    metrics_path: /metrics
    authorization:
      type: Bearer
      credentials: "${TRADING_TERMINAL_METRICS_TOKEN}"
    static_configs:
      - targets:
          - api.example.com
